# GlassSurface provenance

Authoritative local repository:
`/Users/lekan/Dev/glass-projects-lab`

- implementation:
  `react-bits-main/src/ts-default/Components/GlassSurface/GlassSurface.tsx`
- showcase copy:
  `react-bits-glass-surface-showcase/src/vendor/GlassSurface.tsx`
- presets:
  `react-bits-glass-surface-showcase/src/lib/config.ts`

Intentional changes are limited to type-only imports, namespaced CSS and SVG
ids, timeout/observer cleanup, and a wrapper-free portal adapter. Displacement
map generation, filter primitives, defaults, prop behavior, and fallback
behavior remain the source implementation.

The React Bits implementation retains `LICENSE.md`.
