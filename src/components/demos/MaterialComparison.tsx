import { ExperimentShell } from '../layout/WorkspaceShell';

/** Placeholder — side-by-side material comparison will be added later. */
export function MaterialComparison() {
  return (
    <ExperimentShell
      title="Material Comparison"
      technique="Cross-approach preview (not wired yet)"
      referencePaths={[
        '/Users/lekan/Dev/aero-twitter-glass-references/REFERENCE_MANIFEST.md',
      ]}
    />
  );
}
